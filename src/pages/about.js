
const contents = () => {
    let page = ''
    page += '<html>\n'
    page += '<title>About the Weather</title>\n'
    page += '<body>\n'
    page += 'Learn all about the Weather.\n'
    page += '</body>'
    page += '</html>'

    return page
}

module.exports = {
    contents:contents
}